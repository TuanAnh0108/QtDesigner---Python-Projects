import random

num = []
primenum = []
coumt = 0
#Input
for numgiv in range (10):
    r = random.randint(1,30)
    if(r in num):
        random.randint(1,11)
        if(r not in num):
                num.append(r)
    else:
        num.append(r)


print(num)   

#Find the prime factor
def prime(x)
    for div in range(2, x-1)
        if x % div == 0:
            return False
    return True
    
#Find the maximum

print(primenum)
print(max(primenum)) 